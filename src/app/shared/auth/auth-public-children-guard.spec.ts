import { AuthPublicChildrenGuard } from './auth-public-children-guard';

describe('AuthPublicChildrenGuard', () => {
  it('should create an instance', () => {
    // expect(new AuthPublicChildrenGuard()).toBeTruthy();
  });
});
